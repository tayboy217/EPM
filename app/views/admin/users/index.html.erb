<table>
  <tbody>
    <% @users.each do |user| %>
    <tr>
      <td><%= user.name %></td>
      <td><%= user.email %></td>
      <td><%= user.created_at.strftime("%Y年%m月%d日 ") %></td>
      <td><%= link_to "詳細", admin_user_path(user) %></td>
    <% end %>
    </tr>
  </tbody>
</table>
<table>
  <tbody>
    <tr>
      <td><%= @user.name %></td>
      <td><%= @user.email %></td>
      <td><%= @user.created_at.strftime("%Y年%m月%d日 ") %></td>
      <td><%= link_to "編集", edit_user_path(@user) %></td>
    </tr>
  </tbody>
</table>